{
  "name": "@pace/ui",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "bun build browser/index.ts --outfile public/js/components.js --target browser --format iife --minify && bun build browser/app.ts --outfile public/js/app.js --target browser --format esm --minify",
    "dev": "bun run build && bun run server/server.ts",
    "start": "bun run build && bun run server/server.ts"
  },
  "dependencies": {
    "@pace/strava-auth": "workspace:*"
  }
}

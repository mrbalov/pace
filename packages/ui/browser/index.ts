/**
 * Browser entry point for web components.
 * Importing this module registers all components with customElements.
 */

import './components';

// Components are automatically registered via customElements.define() in their respective files
// This file serves as the entry point for bundling all components together
